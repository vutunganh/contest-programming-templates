#include <bits/stdc++.h>

int main() {
  ios_base::sync_with_stdio(false);
  int t;
  cin >> t;
  while (t --) {
    vector<int> dp(100, 0);
    int n;
    cin >> n;
    for (int i = 0; i < n; ++ i) {
      int k;
      cin >> k;
      for (int j = -10; j <= 10; j ++) {

      }
    }
  }
}
